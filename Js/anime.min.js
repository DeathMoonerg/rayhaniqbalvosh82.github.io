import anime from 'animejs';

anime({
  targets: '.animated-webp',
  loop: true, // Set loop to true for infinite animation
  translateX: [0, 250], // Animate from 0 to 250 and back to 0
  duration: 200,
  easing: 'easeInOutSine',
  direction: 'alternate' // Alternate the animation direction
});